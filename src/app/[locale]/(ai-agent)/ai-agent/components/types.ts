/**
 * AI Agent 컴포넌트 타입 정의
 * 공통 타입은 @/types/ai-agent에서 re-export
 */

// 공통 타입 re-export
export type {
  Message,
  MessageRole,
  MessageMetadata,
  UITexts,
  AIAgentResponse,
  AIAgentStatus,
  ExecutionPlanItem,
} from '@/types/ai-agent';
