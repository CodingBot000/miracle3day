/**
 * Onboarding Layout (without BottomNavigation)
 *
 * 온보딩 플로우용 레이아웃 - 하단 네비게이션 없음
 */
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
